'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toBeInTheDOM = toBeInTheDOM;

var _jestMatcherUtils = require('jest-matcher-utils');

var _utils = require('./utils');

function toBeInTheDOM(received) {
  var _this = this;

  if (received) {
    (0, _utils.checkHtmlElement)(received);
  }
  return {
    pass: !!received,
    message: function message() {
      var to = _this.isNot ? 'not to' : 'to';
      return (0, _utils.getMessage)((0, _jestMatcherUtils.matcherHint)(`${_this.isNot ? '.not' : ''}.toBeInTheDOM`, 'element', ''), 'Expected', `element ${to} be present`, 'Received', received);
    }
  };
}